const buttonEditProfile = document.querySelector('.profile__edit-button');

const buttonAddCard = document.querySelector('.profile__add-button');

const buttonEditAvatarProfile = document.querySelector('.profile__button-edit-image');

const editProfilePopupSelector = '.popup_type_profile'

const addCardPopupSelector = '.popup_type_add-card'

const previewImagePopupSelector = '.popup_type_image-view';

const confirmActionPopupSelector = '.popup_type_confirm-delete';

const cardTemplateSelector = '#card-template';

const containerSelector = '.elements';

const editAvatarPopupSelector = '.popup_type_edit-avatar';

export {
  buttonEditProfile,
  buttonAddCard,
  buttonEditAvatarProfile,
  editProfilePopupSelector,
  addCardPopupSelector,
  previewImagePopupSelector,
  confirmActionPopupSelector,
  editAvatarPopupSelector,
  cardTemplateSelector,
  containerSelector };